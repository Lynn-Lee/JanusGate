apiVersion: v1
kind: Service
metadata:
  name: {{ include "janusgate.fullname" . }}
  labels:
    {{- include "janusgate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "janusgate.selectorLabels" . | nindent 4 }}
